import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.pokeapi3.app',
  appName: 'pokeapi3',
  webDir: 'dist', // o 'proyecto' si ahí está el index.html directamente
  bundledWebRuntime: false
};

export default config;

